# ZhPath — передача проекта в новую задачу

Обновлено 14.09.2026. Основа продолжения — `7d776a4`; после неё добавлена голосовая практика.
Это краткая сводка, а не замена исходному коду или полному ТЗ. При расхождениях
сначала проверить текущие файлы, Git и применённые миграции. Не считать старые
сообщения о номерах спринтов подтверждением выполненной работы.

## Проект и текущая точка

Платформа изучения китайского языка **ZhPath**, дипломный проект. Исходный
фронтенд создан пользователем в Figma Make. Язык общения — русский.

- Рабочая папка: `C:/Users/Sultan/Desktop/Diplom`.
- GitHub: https://github.com/goldenwood0/Diploma_work_masterdegree
- Рабочая ветка: `main`. Пользователь просил фиксировать версии и отправлять
  результат в GitHub; не использовать force-push.
- ТЗ было передано по локальному пути
  `C:/Users/Sultan/Documents/Codex/2026-09-08/hsk1-hsk6-3/outputs/TZ_chinese_learning_platform.md`.
  Его наличие и содержание перед уточнением требований нужно проверить.
- План: `docs/ROADMAP.md`. Детальное состояние админки: `docs/SPRINT_6.md`.
- Спринты **0–5 завершены**, **6 приостановлен и не закрыт**, **7 начат**, 8–9 ещё не выполнены.
  Восемь этапов внутри спринта 6 не означают завершение спринта 8.
- Выбранный в плане MVP: один модуль HSK 1 и структура HSK 1–6. Это не весь
  контент HSK 1–6 и не выполнение всего объёма исходного ТЗ.

Последняя работа: независимый черновик опубликованного урока и восстановление
снимков (`821abd7`), затем браузерная проверка всего цикла (`16608d7`). До этого:
CSV (`07da07b`), Excel (`452d8cf`), WAV-медиатека (`e480f4b`).

## Приоритет следующей работы

Актуально после последнего продолжения: в аудиоблоки добавлена локальная запись
голоса (60 секунд / 5 МиБ), плеер, повторная запись/удаление и чек-лист
самопроверки. Запись только по кнопке, без отправки на сервер; освобождение
микрофона и Blob URL при остановке/выходе. Прошли 8 тестов `test:voice`, типы,
467 переводов, сборка, браузерная проверка интерфейса ru/en/kk и ширины 320 px.
На синтетическом сигнале в браузере прошли старт/остановка и создание записи,
но Play привёл к аварийному закрытию вкладки встроенного браузера. Причина не
установлена; воспроизведение и физический микрофон не подтверждены. Поэтому
голосовая практика в ROADMAP остаётся на проверке. Временные тестовые данные
и страницы удалены. Подробности — docs/SPRINT_7.md.
Следующий шаг: проверить полный цикл в обычном браузере, затем тренажёр
иероглифов. Сервер и БД в этом продолжении не менялись.

Предыдущий этап:

Последнее продолжение: серия учебных дней, недельная активность и трудности
по типам заданий на главной. GET `/learning/stats`, таблица StudyActivity,
миграция `20260914120000_study_activity` применена локально. Учебные дни включают
блоки, тесты и SRS; повторы запросов не добавляют дни. Слабые места — последняя
попытка каждого актуального опубликованного теста за 30 дней. Прошли 45 серверных
тестов, типы, 441 перевод, сборка и 7 сценариев рендеринга UI; числовые UTC-смещения
проверены дополнительным целевым API-прогоном. Браузерное подключение не работает,
поэтому интерактивная/визуальная проверка не выполнена. Подробности: docs/SPRINT_7.md.
Следующий шаг: браузерная проверка главной, затем запись/прослушивание голоса
с разрешением пользователя. Спринт 7 остаётся в работе, 6 не закрыт.

Предыдущее продолжение:

Актуальное продолжение 14.09.2026: добавлен поиск по названиям опубликованных
уроков, разделов и программ на ru/kk/en, фильтры HSK и статуса, счётчик и сброс.
Используется существующий каталог; блокировки уроков сохранены. Это поиск по
названиям, не по текстам блоков, словам и грамматике. Проверены frontend TypeScript,
424 перевода, production-сборка и 7 сценариев серверного рендеринга каталога.
После исправления пропущенного перевода проверка i18n прошла. Браузерная проверка
нового интерфейса не выполнялась. Предупреждения Vite о конфиге и бандлах остаются.
Перенесённые задачи спринта 6 явно записаны в ROADMAP; спринт не закрыт.
Следующий шаг: реальные показатели главной — серия учебных дней и слабые места
по результатам упражнений, с учётом часового пояса пользователя.

Ниже — обсуждение приоритетов до этого продолжения:

Пользователь спросил, зачем продолжаем развивать сохранение. Объяснены разные
назначения Git и версий учебного содержания. Последняя рекомендация ассистента:
отложить сравнение снимков и длинную историю как дополнительные удобства,
вернуться к незавершённым основным функциям ТЗ. Конкретная следующая функция
пока не выбрана; сейчас подготовлена передача контекста.

При продолжении сопоставить ТЗ с ROADMAP и предложить ближайшую основную задачу.
Кандидаты из плана: показатели прогресса и слабые места, голосовая/письменная
практика (спринт 7), затем контекстный ИИ-помощник (спринт 8). Не закрывать
спринт 6 молча: если меняется порядок, явно записать перенесённые задачи.
Конец `SPRINT_6.md` содержит прежний следующий шаг — сравнение версий;
это исторический план до обсуждения приоритетов.

## Стек и запуск

- React 19, TypeScript, Vite 8, Tailwind CSS 4; маршруты через hash.
- NestJS 11 с Express, Prisma **6.19.0**, PostgreSQL **17**.
- Zod проверяет входные данные и ответы клиентского API.
- ExcelJS + fflate для словарных `.xlsx`, загружаются динамически.
- CI: Node 22, pnpm **10.34.3**, PostgreSQL 17.
- UI: `http://127.0.0.1:8443`, API: `http://127.0.0.1:3001/api`.
- Vite проксирует `/api` на 3001. Настроенный APP_ORIGIN должен точно совпадать
  с адресом UI: `localhost` и `127.0.0.1` для этой проверки различаются.
- `compose.yaml`: PostgreSQL на `127.0.0.1:5433`, постоянный том
  `postgres-data`; Mailpit SMTP 1025, интерфейс 8025.
- Секреты в игнорируемом `.env`. Не выводить его целиком и не коммитить.

Обычный порядок после настройки `.env` по `.env.example`:

```powershell
npx --yes pnpm@10.34.3 install --frozen-lockfile
docker compose up -d
npx --yes pnpm@10.34.3 db:generate
npx --yes pnpm@10.34.3 db:deploy
npx --yes pnpm@10.34.3 server:build
npx --yes pnpm@10.34.3 db:seed
```

Затем API и Vite запускаются в отдельных процессах командами `server:dev`
и `dev` того же pnpm. Сначала проверить, не заняты ли порты работающим проектом.
`server:dev` запускает **собранный** `dist-server/main.js`, без watch: изменения
сервера требуют сборки и перезапуска. Состояние процессов между задачами не
гарантировано. Не удалять том БД для устранения проблем запуска.

На этой машине глобальный pnpm 12 пытался пересоздавать node_modules и падал
с отказом доступа; восстановлено через pnpm 10.34.3. Не обновлять инструменты
попутно. На Windows Prisma generate может блокироваться работающим API.

## Где искать код

- `src/App.tsx`, `src/main.tsx`, `src/index.css`: приложение и стили.
- `src/pages/`: Auth, Dashboard, Catalog, Lesson, Reviews, Profile,
  ContentAdmin, UsersAdmin, MediaLibrary.
- `src/components/ContentBlocksEditor.tsx`, `ContentQuizEditor.tsx`,
  `VocabularyCsv.tsx`, `MediaPicker.tsx`: редакторы и импорт.
- `src/api/`: клиент API, схемы ответов, CSV/Excel.
- `src/i18n/`: LanguageProvider, словари, включая `learningMessages.ts`.
- `server/app.ts`: Nest-модуль, middleware и глобальный префикс `/api`.
- `server/*controller.ts`: маршруты; `security.ts`: сессии, роли, OriginGuard.
- `server/quiz.ts`: шесть типов заданий, валидация и оценка.
- `server/review.service.ts`: SRS; `server/seed*.ts`: пилотный контент.
- `prisma/schema.prisma`, `prisma/migrations/`: модель и миграции БД.
- `server/auth.test.ts`, `learning.test.ts`, `quiz.test.ts`: тесты.
- `.github/workflows/checks.yml`: CI.

## БД и важные решения

Модели: User, UserSettings, Session, AccountToken, RoleChange;
Curriculum → CourseLevel → CourseUnit → Lesson → LessonBlock;
LessonChange, Quiz, QuizAttempt, LessonProgress; ReviewCard, ReviewEvent;
MediaAsset, MediaChange.

- Роли STUDENT, EDITOR, ADMIN. Регистрация не принимает привилегированную роль.
  Сессии в БД, cookie `zhpath_session` локально / `__Host-zhpath` в production.
  Пароли хешируются; токены хранятся в виде хешей.
- Изменяющие запросы требуют точный Origin и `X-ZhPath-Request: 1`.
- Смена роли проверяет версию, пишет аудит и отзывает сессии; защищены своя
  роль и последний администратор. Реальный аккаунт владельца не выбирать
  произвольно по адресу: в проверках использовались отдельные фикстуры.
- Уроки разблокируются по prerequisite и завершению актуальной версии.
  QuizAttempt хранит снимок результата; отправка защищена от повторов.
- `Lesson.revision` и `Quiz.revision` отражают изменения содержания;
  `Lesson.editVersion` защищает редакторские операции от устаревших записей.
- `Lesson.draft` — независимая JSON-копия title, minutes, blocks, quiz.
  `published=true` может сочетаться с editorialState DRAFT/REVIEW: ученику
  остаётся прежняя публикация. Черновик исключён из ученического ответа.
- Публикация рабочей копии атомарна; блоки и прогресс не сбрасываются при
  неизменном содержании. Изменённый тест сохраняет прежние попытки.
- Восстановление берёт состояние ПОСЛЕ выбранного события, создаёт черновик,
  пишет RESTORE с исходным ID. Автоматической публикации нет.
- Последняя миграция: `20260914120000_study_activity`, применена локально.
- Медиа WAV хранятся байтами в PostgreSQL вместе с авторством/лицензией.
  Архив скрывает файл из обычного выбора, но сохраняет существующие ссылки.

## Основные API

Все пути ниже после `/api`; точные поля смотреть в контроллерах/схемах.

| Область | Маршруты |
| --- | --- |
| Состояние | GET `/health` |
| Аккаунт | POST `/auth/register`, `/login`, `/logout`, `/logout-all`, `/verify-email`, `/resend-verification`, `/forgot-password`, `/reset-password`; GET `/auth/me` (все POST с префиксом `/auth`) |
| Профиль | PATCH `/profile` |
| Обучение | GET `/learning/catalog`, `/learning/lessons/:slug`, `/learning/lessons/:slug/quiz`; PUT `/learning/lessons/:slug/progress`; POST `/learning/lessons/:slug/attempts` |
| Повторение | GET `/reviews`; POST `/reviews/sync`, `/reviews/:id/rate`; PATCH `/reviews/:id` |
| Контент | GET `/content/units`, `/content/lessons`, `/content/lessons/:id`; POST `/content/lessons`; PATCH `/content/lessons/:id`; POST `/content/lessons/:id/state`, `/draft`, `/restore` (последние два также после `:id`) |
| Пользователи | GET `/admin/users`, `/admin/users/:id/roles`; PATCH `/admin/users/:id/role` |
| Медиа | GET/POST `/media`; GET `/media/:id/history`, `/media/:id/file`; PATCH `/media/:id` |

## Что уже работает

Авторизация с email, восстановление доступа, профиль/онбординг, ru/kk/en;
каталог HSK 1–6, четыре пилотных урока и тест модуля; последовательный прогресс,
аудио, шесть типов упражнений и история попыток; словарь, заметки, избранное,
очередь SRS. Админка создаёт уроки, редактирует три типа блоков, задания и
переводы, отправляет на проверку/публикует/архивирует. Есть управление ролями,
WAV-медиатека, CSV/Excel словарного блока, независимый черновик и восстановление.

## Что не готово и ограничения

- ИИ-помощник — заглушка; нет серверного провайдера, лимитов и контекста.
- Спринт 7 выполнен частично (поиск названий, серия дней и слабые места); не выполнены релизная приёмка, полное наполнение и методическая
  проверка HSK 1–6. Наличие фронтенда не подтверждает готовность функции.
- Админка пользователей пока управляет ролями, не полным жизненным циклом
  аккаунта. Медиа — PCM WAV (до 5 МиБ / 5 минут); другие загрузки не реализованы.
- CSV/Excel — слова одного блока, не обмен полными уроками. До 100 слов,
  файл до 1 МБ, ru/kk/en обязательны; Excel — один лист без формул.
- История редактора показывает 30 событий без сравнения и пагинации.
  Неполные старые снимки не восстанавливаются. Откат к отсутствию теста при
  существующем тесте отклоняется. Нет отмены независимого черновика;
  архивирование с таким черновиком недоступно.
- Production-деплой, проверенные резервные копии и мониторинг не подтверждены.
  Известны предупреждения Vite о конфиге и размере бандлов.

## Проверки и передача дальше

Последний полный прогон 12.09.2026: **39 серверных тестов**, frontend/backend
TypeScript, CSV/Excel, **414 переводов**, production-сборка — успешно.
14.09.2026 при подготовке этой сводки тесты заново не запускались.

Браузерная проверка последнего изменения: создание черновика → изменение
названия/текста/ответа → сохранение → проверка → публикация → восстановление →
перезагрузка → повторная публикация. Итог сверён с БД; тестовый аккаунт,
программа и урок удалены. Подробности в `docs/SPRINT_6.md`.

Команды проверки: `typecheck`, `server:build`, `test:server`, `test:i18n`,
`test:csv`, `test:excel`, `build` через pnpm 10.34.3. Перед тестами нужны БД
и миграции. Для документационных изменений повторять весь прогон не требуется.

При обновлении этой сводки записывать дату, опорный коммит, изменённое поведение,
проверки, ограничения и конкретный следующий шаг. Не включать секреты и полную
переписку. В новой задаче сначала прочитать этот файл и проверить Git; не
рассчитывать на автоматический перенос всей истории предыдущей задачи.

Стартовое сообщение для новой задачи:

> Продолжаем ZhPath в C:/Users/Sultan/Desktop/Diplom. Прочитай PROJECT_CONTEXT.md,
> AGENTS.md и актуальный ROADMAP, проверь git status и последние коммиты.
> Сверь оставшиеся основные функции с ТЗ и предложи ближайший шаг. Расширение
> истории версий пока не приоритет. Не считай спринт 6 завершённым.
