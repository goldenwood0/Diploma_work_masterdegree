# Спринт 7 — прогресс и практика

В работе с 14.09.2026. Спринт 6 приостановлен, не закрыт; перенесённые задачи
перечислены в ROADMAP. Спринт 7 целиком не завершён.

## Поиск каталога

Коммит `a9e99ef`: поиск названий программ, разделов и уроков на ru/kk/en,
фильтры HSK и статуса. Поиск внутри блоков, слов и грамматики пока не реализован.

## Серия занятий и слабые места

Добавлен защищённый GET `/api/learning/stats`. Пользователь определяется
по сессии; ответы содержат только его агрегаты, без правильных ответов тестов.

- Учебный день: новое прохождение блока, отправка теста (включая неуспешный)
  или оценка карточки SRS. Просмотр страницы, импорт слов и повтор запроса
  сами по себе не засчитываются.
- Новая таблица StudyActivity хранит время прохождения блоков. Запись и
  обновление прогресса выполняются в одной serializable-транзакции; устаревшие,
  повторные и отклонённые запросы не создают дополнительную активность.
- Для тестов и SRS используются существующие неизменяемые даты событий.
  Миграция `20260914120000_study_activity` переносит только последнюю известную
  дату из каждой записи старого прогресса с nextBlock > 0. Более ранние даты
  восстановить нельзя; это ограничение показано в интерфейсе.
- Серия считается по календарным дням текущего часового пояса профиля. Если
  сегодня занятий ещё нет, серия со вчерашнего дня сохраняется до конца дня.
  После пропущенного дня серия равна нулю. Смена часового пояса пересчитывает
  календарные даты всей истории; также поддерживаются числовые смещения UTC.
- Главная показывает серию, активность последних семи календарных дней и
  отметку сегодняшнего занятия. Статистика обновляется при входе на экран,
  фокусе окна и раз в минуту; ошибка и повторная загрузка обрабатываются отдельно.
- Трудности считаются по последней попытке каждого текущего опубликованного
  теста за последние 30 × 24 часа. Версии урока и теста должны совпадать с
  текущими. Пересдача заменяет прежний результат. Архивные и устаревшие версии
  не входят в анализ ошибок, но факт занятия остаётся в серии.
- Для каждого из шести типов заданий показаны ошибки / ответы и процент.
  Сортировка — по доле ошибок, затем числу ошибок. Это ориентир для практики,
  а не диагностика навыков или оценка уровня HSK. Отсутствие попыток и
  отсутствие ошибок отображаются разными состояниями.

## Проверки 14.09.2026

- Prisma Client сгенерирован, миграция применена к локальной PostgreSQL 17.
- 45 серверных тестов прошли. Новые проверки: сессии, изоляция, повторные
  запросы на следующий день, транзакционная запись блоков, тесты/SRS в серии,
  пересдачи, архивы, версии, окно 30 дней, границы года, високосный день,
  переходы DST, часовые пояса. После доработки числовых смещений UTC повторно
  прошёл целевой API-тест с +23:59 и -23:59.
- Frontend/backend TypeScript, 441 перевод, production-сборка успешны.
- 7 дополнительных сценариев серверного рендеринга UI: ru/kk/en, загрузка,
  ошибка, отсутствие попыток, отсутствие ошибок. Это не интерактивный тест.
- Встроенный браузер не ответил на Page.navigate и Emulation.setFocusEmulationEnabled;
  восстановление через документированные API не помогло. Визуальная,
  мобильная и интерактивная браузерная проверка остаётся открытой.
- Сохраняются прежние предупреждения Vite о конфиге и размере бандлов.

API и Vite запущены локально на 3001 и 8443, Docker Compose — с существующим
томом данных. Сервер требует перезапуска после следующей серверной сборки.

## Следующий шаг

Актуально на 15.09.2026: после расширения поиска добавить очередь SRS на
главную и рекомендацию действия с учётом повторений. Сверка всего объёма:
[TZ_AUDIT_2026-09-15.md](TZ_AUDIT_2026-09-15.md).

Проверить воспроизведение голосовой записи в обычном браузере, включая настоящий
микрофон и разрешения; для письма проверить методически данные и допуски
упражнения на реальных образцах почерка и сенсорных устройствах.
Напоминания, поиск внутри содержания, тренажёр иероглифов, учёт времени занятий
и более подробная диагностика навыков остаются невыполненными.

## Голосовая практика — 14.09.2026

Добавлена в аудиоблок урока рядом с образцом, пиньинем и переводом.
Кнопка явно сообщает о включении микрофона; при загрузке страницы доступа нет.
Запись до 60 секунд / 5 МиБ хранится только как Blob в памяти вкладки.
Есть остановка, отмена ожидания разрешения, повторная запись, удаление,
нативный плеер и чек-лист тонов, слогов и ритма. Чек-лист локальный, оценки
произношения или ASR нет. Записи не отправляются на сервер и не засчитываются
в серию учебных дней сами по себе. Формат выбирает MediaRecorder браузера.

Микрофон освобождается при остановке, ошибке, смене блока/экрана, уходе со
страницы и скрытии вкладки. Результат позднего разрешения после отмены сразу
закрывается. Object URL освобождается при удалении, замене и размонтировании.
При скрытии вкладки начатая запись завершается; при pagehide удаляется.
Образец и собственная запись при запуске приостанавливают друг друга;
образец не запускается во время захвата. Отдельные сообщения для отказа
в разрешении, занятого/отсутствующего устройства, неподдерживаемого браузера,
пустого/слишком большого файла, ошибок кодирования и воспроизведения.

Проверки:

- 8 автоматических тестов управления записью прошли. Команда `test:voice`
  добавлена в package.json и CI; используются имитации устройств и событий.
- Frontend TypeScript, 467 переводов и production-сборка прошли. Сервер не
  менялся; 45 серверных тестов относятся к предыдущему этапу.
- В браузере на отдельном аккаунте проверен аудиоблок ru/en/kk, включая ширину
  320 px. Кнопка и тексты помещаются. Также проверена главная: серия 2 дня,
  недельная активность, часовой пояс и отсутствие попыток.
- Во временной изолированной странице настоящий MediaRecorder записал
  синтетический Web Audio сигнал без обращения к физическому микрофону.
  Подтверждены старт, остановка, состояние ready, плеер и чек-лист.
- При нажатии Play вкладка встроенного браузера показала This page crashed.
  Причина не установлена; успешное воспроизведение не подтверждено. Проверка
  физического микрофона и полного цикла в обычном браузере остаётся открытой;
  пункт ROADMAP поэтому не отмечен завершённым.
- Временная страница и её исходник, тестовые пользователь, программа, урок
  и события активности удалены после проверки. Данные пользователя не менялись.

Предупреждения Vite о конфиге и размере бандлов остаются прежними.

## Свободное письмо — 14.09.2026

После `f71c9e8` (голосовая практика) добавлен CharacterPractice в словарные
блоки уроков. Он извлекает уникальные Han-иероглифы из слов блока, показывает
выбор символа, квадратную сетку и отключаемый полупрозрачный шрифтовой образец.
Рисование через Pointer Events поддерживает мышь, палец и стилус. Есть отмена
последней черты, очистка и локальная отметка самопроверки формы/пропорций.
При смене символа рисунок и отметка сбрасываются; при выходе из блока данные
не сохраняются. Альтернатива для практики без указателя — письмо на бумаге
с тем же образцом. Порядок черт и качество письма автоматически не оцениваются.
Лимиты: 100 черт, до 2000 точек в каждой. Сервер не менялся.

Браузерная проверка на изолированной временной странице: две черты мышью,
скрытие образца, отмена только последней черты, очистка, переключение 你 → 好,
сброс отметки самопроверки. Повторяющиеся иероглифы в списке исключены.
Временные страницы удалены. Frontend TypeScript, 478 переводов и сборка
прошли. Физические сенсорный экран и стилус не проверены. Внешний браузер
не доступен среди подключённых инструментов; проверка воспроизведения голоса
остаётся открытой. Спринт 7 и полный тренажёр письма не закрыты.

## Показ порядка черт — 14.09.2026

После `55e248c` добавлен компонент StrokeOrder внутри CharacterPractice.
14 уникальных иероглифов пилотного словаря: 你好我叫名字什么谢不客气再见.
Данные Hanzi Writer Data из коммита
`68d10a4b21150cae5e1ebbd223eed289cf32d90c`, upstream Make Me a Hanzi,
сохранены без изменения содержимого в public/strokes. Имена файлов — hex
кодпоинты. Полная Arphic Public License, авторство, точный источник и дата
импорта доступны в ARPHICPL.TXT и NOTICE.txt, ссылка есть в интерфейсе.
Данные не добавлены в основной JS-бандл; загружается JSON выбранного символа
с того же сайта. Новая зависимость не потребовалась. Разрешённые символы
ограничены manifest.json; ответ проверяется Zod, обрабатываются ошибка и повтор.

Показ: следующая/предыдущая черта, автоматические шаги раз в секунду,
пауза и сброс. Ничего не запускается автоматически при открытии. При скрытии
вкладки показ останавливается. Смена символа сбрасывает состояние и отменяет
старую загрузку. Текущая черта выделена; круг и квадрат отмечают начало/конец,
пунктир — медиану движения. Это последовательность целых черт, не плавная
анимация кисти. Формат координат преобразован согласно документации источника.
Для символов вне набора — пояснение и доступное свободное письмо.

Проверки: frontend TypeScript, 490 переводов, production-сборка, `test:strokes`
(все 14 файлов, покрытие словаря seed, известные количества черт, неподдерживаемый
символ, повреждённые ответы). Команда добавлена в CI. В браузере проверены
первая черта 你 с началом/концом, автоматическое завершение 7/7, переключение
на 好 со сбросом 0/6, fallback для 龘. Временная страница удалена.
Сервер и БД не менялись. Методическая проверка и автоматическая проверка
написанного остаются открытыми; спринт 7 не закрыт.

## Упражнение по чертам — 14.09.2026

После `dd5346f` добавлен GuidedWriting рядом с показом порядка и свободным
письмом. Поддерживает те же 14 символов; для отсутствующих данных не показывается.
Ученик рисует текущую черту. При отпускании указателя сравниваются длина,
положение и направление с медианой образца. Ошибочная попытка не продвигает
упражнение; принятая заменяется исходной формой черты и открывает следующую.
Обратное направление даёт отдельную подсказку. Есть отключение подсказки,
отмена принятой черты, сброс и сообщение о завершении. Результат локальный,
не сохраняется в аккаунте и не влияет на урок/SRS/серию занятий.

Эвристика в координатах 300×300: 24 точки с равномерным шагом по длине,
отношение длин 0.5–1.8, допуск концов 28, максимальный 40 и средний 20.
Направление выбирается сравнением ошибки прямой и обратной последовательности.
Это помощь для обведения, не оценка почерка: похожие черты могут быть приняты,
необычный правильный почерк может быть отклонён. Допуски не прошли методическую
калибровку. Лимит — 2000 точек на движение; pointercancel не засчитывается.
При смене символа компонент размонтируется, состояние сбрасывается.

Проверки: `test:stroke-matching` проверяет 83 черты набора — точное обведение,
другую плотность точек, небольшой сдвиг, обратный ход, большое смещение и точку;
отдельно зигзаг/возвраты и нечисловой ввод. Исправлено принятие короткой обратной
черты 谢, обнаруженное тестом. Команда включена в CI. `test:strokes`, frontend
TypeScript, 499 переводов и production-сборка прошли. В браузере для 你:
обратная черта отклонена с подсказкой направления; прямая принята (1/7);
отмена возвращает 0/7; черта в другом месте отклонена; сброс и скрытие подсказки
работают. Временная тестовая страница удалена. Полный проход 14 символов
человеком, завершение упражнения в браузере и физический стилус не проверены.

## Поиск внутри доступного содержания — 15.09.2026

GET `/learning/catalog` дополнен полем searchText у урока. Сервер собирает
только разрешённые учебные поля блоков vocabulary/reading/audio: заголовки,
hanzi, пиньинь, ru/kk/en-переводы и текст объяснений. Для пиньиня добавляется
вариант без тоновых знаков с сохранением ü. Черновики, задания теста, правильные
ответы, служебные поля и URL медиа в индекс не входят. Для заблокированного
урока searchText пуст, названия в каталоге доступны как раньше. После завершения
актуальной версии предпосылки следующий каталог включает содержание урока.
Изменение версии предпосылки снова исключает его содержимое из поиска.

Клиент ищет по словам запроса во всех названиях и searchText, учитывая прежние
фильтры HSK/статуса. Например, ni hao находит текст с nǐ hǎo, 女 — блок со словом
女, grammar — объяснение внутри урока, даже если его нет в заголовке.
Навигация ведёт к уроку с обычным восстановлением прогресса, не к найденному
фрагменту. Морфология, релевантность, сниппеты и отдельный словарь грамматики
пока не реализованы. Для небольшого MVP индекс приходит вместе с каталогом;
при росте содержания следует перенести поиск в отдельный пагинируемый endpoint.

Проверки: 46 серверных тестов, frontend/backend TypeScript, 500 переводов,
production-сборка. Новый API-тест проверяет исключение скрытых полей, черновиков,
заблокированного содержания, разблокировку, изоляцию учеников и смену ревизии.
Дополнительно прошли 7 сценариев рендеринга каталога и проверки сборки текста
для всех трёх типов блоков. Интерактивная браузерная проверка этого изменения
не выполнялась. Предупреждения Vite прежние. Миграции не нужны; API и Vite
перезапущены на 3001/8443. Docker API в сессии недоступен, но существующая
PostgreSQL на 5433 работает и все API-тесты прошли; том данных не менялся.
